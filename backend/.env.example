# Configuración del Servidor
PORT=3001
NODE_ENV=production

# Base de datos PostgreSQL (Railway la generará automáticamente)
DATABASE_URL=postgresql://postgres:password@localhost:5432/censo_municipal

# Frontend URL (Vercel la generará automáticamente)
FRONTEND_URL=https://tu-app.vercel.app
